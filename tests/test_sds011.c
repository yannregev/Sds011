#include "unity.h"


// Runs before EACH test case
void setUp(void) {
    // e.g. reset state, allocate fixtures
}

// Runs after EACH test case
void tearDown(void) {
    // e.g. free fixtures
}



int main(void) {
    UNITY_BEGIN();

    return UNITY_END();
}